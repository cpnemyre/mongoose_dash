<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Quotes Page</title>
  </head>
  <body>
    <h1>List of Tigers</h1>
    <a href="/addnew"><button>Add new tiger</button></a>
    <div id="container">
    <div>
      <% for(var tiger in tigers) { %>
      <p> Name: <%= tigers[tiger].name  %></p>
      <p> Color: <%= tigers[tiger].color %> </p>
      <p> Gender: <%= tigers[tiger].gender %> </p>
      <a href="/view/<%= tigers[tiger]._id %>"><button>View this tiger</button></a>
      <form action="/destroy/<%= tigers[tiger]._id %>" method="post">
        <button>Delete Tiger</button>
      </form>
  <hr>
    </div>
  <% } %>
  </body>
</html>
